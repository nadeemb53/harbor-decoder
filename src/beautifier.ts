import { ethers } from "ethers";
import { Path } from "./interface/path.enum";

export class Beautifier {
    beautify(tx: ethers.providers.TransactionResponse, path: Path){
        return '';
    }
}